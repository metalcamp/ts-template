setup:
    #addons:
    #  - plan: heroku-postgresql:hobby-dev
    #    version: 11
    #    as: DATABASE
    config:
        APP_ENV: "prod"
        TZ: "Europe/Berlin"
build:
    docker:
        web: Dockerfile
